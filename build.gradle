plugins { 
  id "com.marklogic.ml-gradle" version "3.9.0"
  id "java"
  
}

task projectInfo {
    doLast {
    def name = project.name
    def projectDir = project.projectDir
println 'Project name:' + name
println 'projectDir: '+ projectDir
println 'projectDir list files:'+ projectDir.listFiles()



    }
}